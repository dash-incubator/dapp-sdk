import { Connection, ContractTransition, Definition } from "../types";
declare const _default: ({ client, identity }: Connection, definition: Definition) => Promise<ContractTransition>;
export default _default;
