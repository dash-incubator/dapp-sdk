import { Connection, Documents, Document } from "../types";
declare const _default: ({ client, identity }: Connection, documents: Documents | Document) => Promise<Documents>;
export default _default;
