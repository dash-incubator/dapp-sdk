import { Connection, Document, Documents } from "../types";
declare const _default: ({ client, identity }: Connection, documents: Documents | Document, locator: string) => Promise<Documents>;
export default _default;
