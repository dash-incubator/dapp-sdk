import config from './config';
import entity from './entity';
import storage from './storage';
import user from './user';


export default { config, entity, storage, user };
export { config, entity, storage, user };
